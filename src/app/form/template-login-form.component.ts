import { Component } from "@angular/core";
import { NgModel } from "@angular/forms";

@Component({
  selector: "template-login-form",
  templateUrl: "./template-login-form.component.html"
})
export class TemplateLoginFormComponent {
  public onSubmit(loginForm) {
    debugger;
    loginForm;
  }
}
